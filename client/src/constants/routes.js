const PublicRoutes = {
  LOGIN: '/login',
  SIGN_UP: '/sign-up',
  PROFILE: '/profile/:username',
  NOT_FOUND: '/not-found'
}

const AdminRoutes = {
  ADMIN: '/admin',
  ADMIN_LOGIN: '/admin/login'
}

export { PublicRoutes, AdminRoutes }
